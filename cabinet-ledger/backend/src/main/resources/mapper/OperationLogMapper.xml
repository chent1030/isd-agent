<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cabinet.mapper.OperationLogMapper">

    <insert id="insert" parameterType="com.cabinet.entity.OperationLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO operation_log
        (cabinet_id, operator, action, detail, ip_addr, created_at)
        VALUES
        (#{cabinetId}, #{operator}, #{action}, #{detail}, #{ipAddr}, #{createdAt})
    </insert>

    <select id="selectLogList" resultType="com.cabinet.entity.OperationLog">
        SELECT
            id,
            cabinet_id,
            operator,
            action,
            detail,
            ip_addr,
            created_at
        FROM operation_log
        WHERE 1 = 1
        <if test="cabinetId != null and cabinetId != ''">
            AND cabinet_id = #{cabinetId}
        </if>
        <if test="operator != null and operator != ''">
            AND operator = #{operator}
        </if>
        ORDER BY created_at DESC, id DESC
    </select>

</mapper>
